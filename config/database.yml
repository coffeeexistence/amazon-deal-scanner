# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  encoding: unicode
  database: fba_default
  pool: 5
  username: fba
  password:
  host: localhost

development:
  adapter: postgresql
  encoding: unicode
  database: fba_development
  pool: 5
  username: fba
  password:
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: postgresql
    encoding: unicode
    database: fba_test
    pool: 5
    username: fba
    password:
    host: localhost

production:
    adapter: postgresql
    encoding: unicode
    database: fba_production
    pool: 5
    username: fba
    password:
    host: localhost
